name: Render Quarto PDFs in Paper2 and Commit

on:
  push:
    branches:
      - main
    paths:
      - 'Paper2/Paper/*.qmd'
      - 'Paper2/Paper/preamble2.tex'
      - 'Paper2/Paper/pl.bib'

permissions:
  contents: write

jobs:
  render-and-commit-pdfs:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Quarto with TinyTeX
        uses: quarto-dev/quarto-actions/setup@v2
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tinytex: true

      - name: Render Quarto files to PDF
        run: |
          for file in Paper2/Paper/*.qmd; do
            if [ -f "$file" ]; then
              quarto render "$file" --to pdf
            fi
          done

      - name: Commit PDF files
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'Add rendered PDFs from Paper2'
          file_pattern: 'Paper2/Paper/*.pdf'
          branch: main
